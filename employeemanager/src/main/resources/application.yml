spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employeeManager
    password: password
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect